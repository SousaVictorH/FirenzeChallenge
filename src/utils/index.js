/**
 * Essa função formata o link passado como parametro e o retorna
 *
 * @param {String} link          Lnik
 * @returns {String}
 */
export const formatLink = link => {
  return link.substr(1);
};
